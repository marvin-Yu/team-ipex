# team-ipex
```shell
pip install -r requirements.txt

usage: demo.py [-h] [--cpu] [--gpu] [--ipex] [--dtype {fp32,fp16,bf16}] [--iters ITERS]

optional arguments:
  -h, --help            show this help message and exit
  --cpu                 Use CPU (default)
  --gpu                 Use GPU
  --ipex                Use IPEX
  --dtype {fp32,fp16,bf16}
                        Data type
  --iters ITERS         Number of iterations (default is 100)
```